<% provide(:title, @taxon.name) %>
<!-- LIGHT SECTION -->
<%= render 'light_section_pageheader', taxon: @taxon %>
<!-- MAIN CONTENT SECTION -->
<%= render 'maincontent_section', products: @products %>
<!-- LIGHT SECTION -->
<%= render 'light_section' %>